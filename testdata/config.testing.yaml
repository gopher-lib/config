port: 8080
secret1: ${SOME_SECRET}
secret2: ${SECRET}
dollar: $$money
db:
  user: root
  password: ${DB_PASSWORD}
connectionString: "${DB_USER}:${DB_PASSWORD}@tcp(${DB_HOST}:${DB_PORT})/${DB_NAME}?parseTime=true"
